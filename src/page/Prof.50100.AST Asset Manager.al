profile "AST-ASSET-MANAGER"
{
    Caption = 'Asset Manager';
    ProfileDescription = 'Role for managing company assets, assignments and returns.';
    RoleCenter = "AST Asset Tracking Role Center";
    Enabled = true;
    // This Profile makes the Role Center selectable in BC under
    // Settings → Personalization → Role. Without this object,
    // the Role Center page exists but no user can be assigned to it.
}
